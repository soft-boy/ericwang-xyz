{
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "deploy": "next build && sudo rm -rf ~/www/* && sudo cp -r out/* ~/www/ && sudo nginx -s reload"
  },
  "dependencies": {
    "next": "latest",
    "next-use-posthog": "^1.16.0",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  }
}
